name: Test Hello World Action

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello World Echo
        run: |
          echo "Hello World! Action شغال تمام 🚀"
          echo "Repo: ${{ github.repository }}"
          date
      - name: List files
        run: ls -la
